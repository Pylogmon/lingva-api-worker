{
  "name": "lingva-rest-api-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "tsc --noEmit",
    "dry-run": "wrangler deploy --dry-run"
  },
  "dependencies": {
    "lingva-scraper": "1.1.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "wrangler": "^4.90.0"
  }
}
